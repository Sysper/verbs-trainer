/* ═══════════════════════════════════════════════════════════════
   ⚙️ EDIT THIS — put your own usernames here.
   Leave a value as '' (empty) and its button disappears automatically.
   ═══════════════════════════════════════════════════════════════ */
export const CONFIG = {
  githubUser: 'sysper',          // your GitHub username
  githubRepo: 'verbs-trainer',   // ← change if your repo has another name

  // Donations — leave '' to hide a button.
  kofi: 'tartadeguayaba',        // ko-fi.com/tartadeguayaba
  nequi: '3506922548',           // Nequi — copies the number + opens the PSE page
  breb: '@CSV260',               // Bre-B key — tap copies it
} as const;

export const NEQUI_URL = 'https://clientes.nequi.com.co/recargas';
